mutation CreateLayer($name: String!, $description: String!, $settings: JSON!, $mapId: ID!) {
    createLayer(layerInput: { name: $name, description: $description, settings: $settings}, mapId: $mapId) {
        _id
        name
        description
        settings
    }
}
