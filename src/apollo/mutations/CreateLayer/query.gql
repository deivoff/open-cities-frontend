mutation CreateLayer($name: String!, $description: String!, $properties: [JSON!]!, $mapId: ID!) {
    createLayer(layerInput: { name: $name, description: $description, properties: $properties}, mapId: $mapId) {
        _id
        name
        description
        settings
    }
}
