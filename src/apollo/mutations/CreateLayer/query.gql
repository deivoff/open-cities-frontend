mutation CreateLayer($name: String!, $description: String!, $configuration: JSON!, $mapId: ObjectId!) {
    createLayer(layerInput: { name: $name, description: $description, configuration: $configuration}, mapId: $mapId) {
        _id
        name
        description
        configuration
        access
        owner {
            role
        }
    }
}
