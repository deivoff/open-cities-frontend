mutation CreateGeo($geometry: GeometryInput!, $settings: JSON!, $layer: ID!) {
    createGeo(geoInput: {geometry: $geometry, settings: $settings, layer: $layer}) {
        _id
        geometry {
            type
            coordinates
        }
        settings
    }
}
