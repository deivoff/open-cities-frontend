mutation CreateMap($mapSettings: MapInput!, $type: AccessType) {
    createMap(mapInput: $mapSettings, type: $type) {
        name
        description
        settings {
            bbox
            zoom
        }
        access
    }
}
